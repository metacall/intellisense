export function mytsFunction(b: number) : number {
    return b;
}

export function myFunction22(b: string, c: number[]) : string {
    for(const num of c) {
        console.log(num);
    }
    return b
}